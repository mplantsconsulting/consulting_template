# SETUP — 案件開始の手順

この雛形を新しい案件で使い始めるまでの手順。**最初の30分で終わる範囲**に絞っている。

---

## 1. リポジトリを作る（5分）

```bash
# GitHub のテンプレート機能を使う場合はそちらが早い
git clone https://github.com/mplantsconsulting/consulting_template.git <案件名>
cd <案件名>
rm -rf .git && git init
```

案件名は `<クライアント略称>_<テーマ>` 程度で十分（例: `acme_churn`）。

```bash
uv sync   # 依存関係の取得
```

`pyproject.toml` の `name` / `description` を案件名に書き換える。

---

## 2. CLAUDE.md を書く（10分）— これが Step 0

[CLAUDE.md](CLAUDE.md) の冒頭「目的」ブロックだけを書き換える。以下の3点を埋める。

| 項目 | 書けているかの判定 |
|---|---|
| **KGI** | 〈誰の〉〈どの状態を〉〈どう変えるか〉が1文で書けている |
| **意思決定者と、動かしたい意思決定** | 「誰が何を決めるための材料か」が名指しで書けている |
| **期限と提出形態** | 提出日と成果物の形が決まっている |

**ここが書けないまま先に進まない。** 依頼文はまだ問いになっていない。
「未収を減らしたい」は依頼であって問いではなく、**問いに変換する作業が最初の仕事**である。

役割・禁止事項の部分は原則そのまま使う。案件特有の禁止事項があれば追記する。

---

## 3. 情報源の棚卸しをする（10分）— Step 1 の入口

`docs/business/` に、**まだ中身は空でよいので**骨組みだけ作る。

1. [docs/business/_TEMPLATE_flow.md](docs/business/_TEMPLATE_flow.md) をコピーして、業務フローの空の骨組みを作る
   （各段階に `【仮説・未確認】` を付けた状態でよい。**むしろそれが正しい初期状態**）
2. [docs/business/ref_data_inventory.md](docs/business/ref_data_inventory.md) に、受領済み・受領予定のデータを列挙する
3. 手に入る**一次資料**（契約書・約款・規程・公開IR）を洗い出して一覧にする

情報源の階層（一次／二次／三次／参考）の考え方は [docs/business/README.md](docs/business/README.md) にある。
**先に読むこと。** 「何を事実として引用してよいか」がここで決まる。

---

## 4. INDEX.md を起動する（5分）

[docs/INDEX.md](docs/INDEX.md) の冒頭を案件名に書き換える。
以降、**ファイルを新規作成・更新したら必ずここに反映する。** 索引のないドキュメント群は、増えた瞬間に誰も読まなくなる。

---

## 5. 最初のコミット

```bash
git add -A
git commit -m "chore: 案件テンプレートの初期設定（KGI・情報源の骨組み）"
```

---

## ここから先

- 進め方の全体像: **[docs/WORKFLOW.md](docs/WORKFLOW.md)**
- 提出前に必ず通すチェック: **[docs/QUALITY_BAR.md](docs/QUALITY_BAR.md)**
- Claude の使い方: **[.claude/README.md](.claude/README.md)**

### 最初の1週間の目安

| 日 | やること | Step |
|---|---|---|
| 1 | KGI の確定、一次資料の収集依頼、データ受領の確認 | 0 / 1 |
| 2-3 | 一次資料から業務フローを図にする。ヒアリング項目を作る | 1 |
| 3-4 | 受領データの変数定義・形状・結合キーを台帳化 | 2 |
| 4-5 | **指標の定義を1ファイルに固定する**（最重要） | 2 |
| 5 | 問題を構造化し、空欄の一覧を作る。分析1本目の計画を書く | 3 / 4 |

**この1週間で集計結果が1つも出ていなくても問題ない。**
コードを書いていない期間があることを不安に思わなくてよい。
分析はいつでも回せるが、業務理解とデータ定義を飛ばすと**あとの全部が無駄になる。**
